<UserControl x:Class="FinGameAssistant.Views.TransactionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:FinGameAssistant.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#F5F7FA">

    <UserControl.Resources>
        <converters:TypeToColorConverter x:Key="TypeToColorConverter"/>
        <converters:CategoryIconConverter x:Key="CategoryIconConverter"/>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>

        <Style x:Key="FilterStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" Opacity="0.05" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" 
                       Text="История операций" 
                       Style="{StaticResource HeaderStyle}"/>

            <Button Grid.Column="1" 
                    Content="➕ Добавить операцию"
                    Background="#3498DB"
                    Foreground="White"
                    Style="{StaticResource ActionButtonStyle}"
                    Command="{Binding OpenAddTransactionCommand}"
                    Width="180"/>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource FilterStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Период:" VerticalAlignment="Center" FontWeight="SemiBold"/>

                    <ComboBox Grid.Column="2" 
              ItemsSource="{Binding DateFilters}"
              SelectedItem="{Binding SelectedDateFilter}"
              DisplayMemberPath="Name"
              Height="35" FontSize="13"/>

                    <TextBlock Grid.Column="4" Text="Тип:" VerticalAlignment="Center" FontWeight="SemiBold"/>

                    <ComboBox Grid.Column="5" 
              ItemsSource="{Binding TypeFilters}"
              SelectedItem="{Binding SelectedTypeFilter}"
              DisplayMemberPath="Name"
              Height="35" FontSize="13"/>
                </Grid>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding FilteredTransactions, UpdateSourceTrigger=PropertyChanged}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          BorderBrush="#EEEEEE"
                          BorderThickness="1"
                          RowHeaderWidth="0"
                          Background="White"
                          FontSize="13"
                          RowHeight="40"
                          AlternatingRowBackground="#F9F9F9"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F0F0F0"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Height" Value="40"/>
                            <Setter Property="BorderBrush" Value="#DDDDDD"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="5,0"/>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header=" " Width="40">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Category, Converter={StaticResource CategoryIconConverter}}"
                                               FontSize="18"
                                               HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Дата" Width="100" 
                                          Binding="{Binding Date, StringFormat=dd.MM.yyyy}"/>

                        <DataGridTextColumn Header="Категория" Width="150" 
                                          Binding="{Binding Category}"/>

                        <DataGridTextColumn Header="Описание" Width="200" 
                                          Binding="{Binding Description}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="NoWrap"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="Сумма" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Amount, StringFormat=N2}"
                                               Foreground="{Binding Type, Converter={StaticResource TypeToColorConverter}}"
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Right"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header=" " Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="✏️" 
                                                Background="Transparent"
                                                Foreground="#3498DB"
                                                FontSize="14"
                                                Margin="2"
                                                Command="{Binding DataContext.EditTransactionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"/>
                                        <Button Content="🗑️" 
                                                Background="Transparent"
                                                Foreground="#E74C3C"
                                                FontSize="14"
                                                Margin="2"
                                                Command="{Binding DataContext.DeleteTransactionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <Border Grid.Row="2" 
                        Background="#F0F0F0" 
                        CornerRadius="5" 
                        Padding="15"
                        Margin="0,10,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" 
                                   Text="Итого за выбранный период:" 
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>

                        <TextBlock Grid.Column="2" 
                                   Text="{Binding TotalIncome, StringFormat='Доходы: {0:N2} ₽'}" 
                                   Foreground="#27AE60"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Right"/>

                        <TextBlock Grid.Column="3" 
                                   Text="|" 
                                   Margin="10,0"
                                   Foreground="#BDC3C7"/>

                        <TextBlock Grid.Column="4" 
                                   Text="{Binding TotalExpense, StringFormat='Расходы: {0:N2} ₽'}" 
                                   Foreground="#E74C3C"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Right"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>