<UserControl x:Class="FinGameAssistant.Views.AchievementsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:FinGameAssistant.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#F5F7FA">

    <UserControl.Resources>
        <converters:BooleanToBackgroundConverter x:Key="BooleanToBackgroundConverter"/>
        <converters:BooleanToIconBackgroundConverter x:Key="BooleanToIconBackgroundConverter"/>
        <converters:InvertedBooleanToVisibilityConverter x:Key="InvertedBooleanToVisibilityConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="AchievementCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" Opacity="0.05" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Достижения" 
                   Style="{StaticResource HeaderStyle}"
                   Margin="0,0,0,10"/>

        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="10" 
                Padding="20"
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                    <TextBlock Text="⭐" FontSize="24" Margin="0,0,10,0"/>
                    <StackPanel>
                        <TextBlock Text="Текущий уровень" FontSize="11" Foreground="#7F8C8D"/>
                        <TextBlock>
                            <Run Text="{Binding CurrentLevel}" FontSize="20" FontWeight="Bold" Foreground="#2C3E50"/>
                            <Run Text=" - " FontSize="14" Foreground="#7F8C8D"/>
                            <Run Text="{Binding CurrentLevelName}" FontSize="14" Foreground="#3498DB"/>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" 
                                   Text="Прогресс до следующего уровня" 
                                   FontSize="11" 
                                   Foreground="#7F8C8D"/>
                        <TextBlock Grid.Column="1" 
                                   Text="{Binding ExperienceProgress}" 
                                   FontSize="11" 
                                   FontWeight="SemiBold" 
                                   Foreground="#2C3E50"/>
                    </Grid>
                    <ProgressBar Value="{Binding ExperiencePercentage}" 
                                 Height="8"
                                 Foreground="#3498DB"
                                 Background="#E0E0E0"
                                 Margin="0,5,0,0"/>
                    <TextBlock Text="{Binding NextLevelName, StringFormat='Следующий уровень: {0}'}" 
                               FontSize="10" 
                               Foreground="#7F8C8D"
                               Margin="0,2,0,0"
                               HorizontalAlignment="Right"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="20,0,0,0">
                    <TextBlock Text="🏆" FontSize="24" Margin="0,0,10,0"/>
                    <StackPanel>
                        <TextBlock Text="Достижения" FontSize="11" Foreground="#7F8C8D"/>
                        <TextBlock Text="{Binding UnlockedAchievementsCount, StringFormat='{}{0}/5'}" 
                                   FontSize="20" FontWeight="Bold" Foreground="#2C3E50"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Achievements}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource AchievementCardStyle}"
                                Background="{Binding IsUnlocked, Converter={StaticResource BooleanToBackgroundConverter}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" 
                                        Width="50" Height="50"
                                        Background="{Binding IsUnlocked, Converter={StaticResource BooleanToIconBackgroundConverter}}"
                                        CornerRadius="25"
                                        Margin="0,0,15,0">
                                    <TextBlock Text="{Binding Icon}" 
                                               FontSize="24"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                                
                                <StackPanel Grid.Column="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" 
                                                   Text="{Binding Name}" 
                                                   FontSize="14" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="#2C3E50"/>
                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding ExperienceReward, StringFormat='+{0} XP'}" 
                                                   FontSize="12" 
                                                   Foreground="#3498DB"
                                                   FontWeight="SemiBold"/>
                                    </Grid>

                                    <TextBlock Text="{Binding Description}" 
                                               FontSize="12" 
                                               Foreground="#7F8C8D"
                                               Margin="0,2,0,2"/>

                                    <Grid Visibility="{Binding IsUnlocked, Converter={StaticResource InvertedBooleanToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ProgressBar Grid.Column="0" 
                                                     Value="{Binding Progress}" 
                                                     Maximum="{Binding Target}"
                                                     Height="4"
                                                     Foreground="#3498DB"
                                                     Background="#E0E0E0"
                                                     Margin="0,5,5,0"/>
                                        <TextBlock Grid.Column="1" 
                                                   Text="{Binding Progress, StringFormat='{}{0}/{1}'}" 
                                                   FontSize="10" 
                                                   Foreground="#95A5A6"/>
                                    </Grid>

                                    <TextBlock Visibility="{Binding IsUnlocked, Converter={StaticResource BooleanToVisibilityConverter}}"
                                               Text="{Binding UnlockedAt, StringFormat='Получено: dd.MM.yyyy'}" 
                                               FontSize="10" 
                                               Foreground="#27AE60"/>
                                </StackPanel>

                                <TextBlock Grid.Column="2" 
                                           Text="✓"
                                           Visibility="{Binding IsUnlocked, Converter={StaticResource BooleanToVisibilityConverter}}"
                                           FontSize="24"
                                           Foreground="#27AE60"
                                           Margin="10,0,0,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>